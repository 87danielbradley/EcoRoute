import React from "react";
import { connect } from "react-redux";
import LoginGeocoder from "./login_geocoder"

const mSTP = (state ={}, ownProps) => ({
    
})

const mDTP = (dispatch, ownProps) => ({
    
})

export default connect(mSTP,mDTP)(LoginGeocoder);